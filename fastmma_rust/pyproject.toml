[build-system]
requires = ["maturin>=1.13,<2.0"]
build-backend = "maturin"

[project]
name = "fastmma_rust"
version = "0.0.1"
requires-python = ">=3.10"

[tool.maturin]
module-name = "fastmma_rust"
features = ["pyo3/extension-module"]
